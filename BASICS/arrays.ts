const gods: string[] = [];

const oddnumbers: number[] = [];

const otherway: Array<number>=[]//// other way of defineing a array

type User={
    name:string
    email:string
}

const allusers: User[]=[]

const MLModes:number[][]=[
    [255, 255,255],
    [],
    // "dasd"
]

oddnumbers.push(1);

gods.push("krishna");

allusers.push({name:"",email:""})